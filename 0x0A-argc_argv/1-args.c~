#include <stdio.h>

/**
 * main - prints arguments count
 * @argc: argument count
 * @argv: argument vector
 * Return: always 0
 **/

int main(int argc, __argument__((unused)) char **argv)
{
	printf("%i\n", argc);
	return (0);
}
